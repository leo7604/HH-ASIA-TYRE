{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ],
  "env": {
    "NOTE": "Environment variables must be set in Vercel Dashboard",
    "REQUIRED_VARIABLES": [
      "VITE_SUPABASE_URL",
      "VITE_SUPABASE_ANON_KEY"
    ],
    "INSTRUCTIONS": "Go to Vercel Dashboard > Settings > Environment Variables and add the Supabase credentials"
  }
}
